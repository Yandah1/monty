#include "monty.h"

/**
 * nop - does nothing
 * @stack : the pointer to the stack
 * @line_number: line number where nopis encounted
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
