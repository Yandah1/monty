#include "monty.h"

/**
 * nop - doesn't do anything
 * @stack: the pointer to the stack
 * @line_number: line number where nope is encounted
 */

void _nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
